# CWAC

Project context lives in `docs/`. It is loaded here so every session starts
with it:

@docs/parent.md

- Before working on a feature, read its file in `docs/` rather than exploring
  the code.
- When you add or change a feature, write or update its `docs/<feature>.md`
  and the index in `docs/parent.md` in the same change.
