# cwac-one

#have the loggone one ame ion the logging function so you dont have to import it everytime
#the success meaage must be basd upon a real use case discussed
#move the doctor model schema to the fromt so the processing happens in the logic function
#i add_doctor .py make function of functions agains that doctor model
#object_serializer
#doctor crud

To run the uvicorn server :
.\.venv\Scripts\python.exe -m uvicorn main:app --reload
To run the vite server :
npm run dev
