# MERGED requirements — review, then rename over the top of requirements.txt
# once cwacone's files are in place.
#
# Everything above the divider is your existing requirements.txt, byte for byte
# and in the same order, so a diff against the original shows ONLY additions.
# Where a package existed in both projects, your pin won — nothing you already
# depend on moves version.
#
#   .venv\Scripts\python.exe -m pip install -r requirements.txt

annotated-doc==0.0.4
annotated-types==0.8.0
anyio==4.14.2
bcrypt==5.0.0
click==8.4.2
colorama==0.4.6
dnspython==2.8.0
email-validator==2.3.0
fastapi==0.140.0
h11==0.16.0
httptools==0.8.0
idna==3.18
pydantic==2.13.4
pydantic-settings==2.14.2
pydantic_core==2.46.4
PyJWT==2.13.0
pymongo==4.17.0
python-dotenv==1.2.2
PyYAML==6.0.3
starlette==1.3.1
typing-inspection==0.4.2
typing_extensions==4.16.0
uvicorn==0.51.0
watchfiles==1.2.0
websockets==16.1.1

# ---------------------------------------------------------------------------
# Added by the chatbot package
# ---------------------------------------------------------------------------

# The LLM fallback (ENG-1659). anyio, pydantic and typing_extensions are among
# its dependencies and you already pin all three — no version change needed.
openai==3.6.0

# openai 3.6.0's own dependencies, which nothing else here pulls in.
httpx2==2.12.0
httpcore2==2.12.0
jiter==0.16.0
sniffio==1.3.1
truststore==0.10.4

# NOT currently in your requirements, and the Twilio webhook will not work
# without it: Twilio posts the WhatsApp payload as form-encoded data, and
# FastAPI's Form(...) raises at import time if this is missing. Worth
# installing before the account arrives rather than debugging it live.
python-multipart==0.0.32
